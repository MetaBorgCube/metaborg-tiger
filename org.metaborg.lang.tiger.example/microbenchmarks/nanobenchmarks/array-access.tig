let
  type intArray = array of int
  var arr : intArray := intArray[10000] of 42
in
  for i := 0 to 30
    do
      (
      timeGo();
      let
        var x : int := 0
      in
        for r := 0 to 9999
        do
          x := arr[r]
      end;
      timeStop()
    )
 end