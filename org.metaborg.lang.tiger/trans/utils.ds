module utils

signature

  native operators
    _printTerm : AST -> AST
    _printSTerm: AST -> AST
  
  arrows
    printTerm(AST) --> AST
    printSTerm(AST) --> AST
  
rules

  printTerm(tree) --> _printTerm(tree)
  
  printSTerm(tree) --> _printSTerm(tree)
