module dynamics-frames-directed/types

imports
  dynamics-frames-directed/base
  dynamics-frames-directed/variables  

signature
  arrows
    F |- Map-TypeDecs(List(TypeDec)) --> U
    TypeId --> Type
    
rules // type declarations

  Frames1 (F, F_outer) |- Dec(TypeDecs(_)) --> U()

rules // types
  
  Tid(x : Occurrence) --> typeOf(declOfPath(pathOf(x)))
  
  